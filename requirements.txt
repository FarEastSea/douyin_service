# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
python-multipart==0.0.6

# 任务队列
celery[redis]==5.3.4
redis==4.6.0

# 数据库
sqlalchemy==2.0.25

# PostgreSQL 驱动
asyncpg==0.30.0
psycopg2-binary==2.9.9

# MySQL 驱动（可选）
aiomysql==0.2.0
pymysql==1.1.0

# HTTP 请求
requests==2.31.0
httpx==0.26.0

# 数据验证
pydantic==2.5.3
pydantic-settings==2.1.0

# 工具
python-dotenv==1.0.0
# 与宝塔统一环境中的 pyOpenSSL 26.x / service-identity 兼容。
cryptography>=49,<50
gallery-dl>=1.30,<2.0
# 抖音 Web API a_bogus 签名使用；算法实现来源见 THIRD_PARTY_NOTICES.md。
gmssl>=3.2.2,<4.0
# gallery-dl 的微博 HLS 视频和后续 B站适配共用该媒体引擎。
yt-dlp>=2026.8.19,<2027

# 版本更新（纯 Python git，规避受限环境无法执行 shell 的问题）
dulwich>=1.0,<2.0

# WebSocket (可选，用于实时进度推送)
websockets==12.0

