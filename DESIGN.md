# 媒体控制台设计规范

## 产品与界面目标

个人管理员的工作顺序是提交下载、确认队列、处理失败、查看订阅结果，最后进入系统维护。界面采用“值班工作簿”：用清晰的标题、分隔线和可扫描的记录组织信息；平台分类是补充入口，不再主导首页。`PRODUCT.md` 是产品定位和能力边界的依据。

## 信息架构

1. 工作台：全局下载入口、全部平台任务概览、真正需要人工处理的异常、最近订阅检查与任务。
2. 全部任务：跨平台分页、筛选、精确任务选择、失败诊断、预览与重试。
3. 作者与作品：抖音作者订阅、作者定位与高密度作品库。
4. 自动更新：抖音检查周期、异常证据、复制诊断与人工触发。
5. 平台专项：保留各平台已存在的任务/用户操作；小红书仅允许单条笔记。
6. 运行维护：服务进程、平台就绪、存储巡检、日志和诊断。
7. 设置：常规配置、平台账号、下载与风控、归档与导出。网页持久化值仍是配置权威来源。

旧平台设置链接按原 `tab` 类型转到“设置”或“运行维护”；根路由进入工作台。

## 视觉系统

- 字体：系统无衬线字体；正文 14px / 1.5，表格与辅助信息原则上不低于 12px，页面主标题 24–29px。
- 色彩：深色以冷静的蓝黑、石板灰为基础；浅色以暖中性的浅灰与白色为基础。钴蓝只用于主操作和选中态。绿色、琥珀色与红色分别表示成功、需关注、失败，并始终配合文字。
- 间距：4px 基础节奏，按钮高度约 38px，列表行最少约 62px。数据区域用细分隔线保持密度，避免重复套卡。
- 形状：主要区域 10–12px 圆角，控件 7–8px；不使用大面积渐变或装饰阴影。
- 主题：`data-theme` 控制深浅主题，支持跟随系统。颜色、边线和文字均来自 `frontend/src/workspace.css` 令牌。

## 核心组件与状态

- 全局下载侧栏：先输入、识别平台、确认来源，再提交。识别失败时允许手动选平台；小红书作者主页明确拒绝。抖音已存在作者会跳转并高亮原记录。
- 工作台计数明确标注“全部平台”；接口未返回时显示“—”和失败提示，不推断为零。普通排队任务不会进入“需要你处理”。
- 表格桌面保留列标题与高密度行；窄屏改为逐行字段标签。筛选搜索仍由服务端分页负责，批量操作只针对显式选中的任务 ID。
- 失败状态把原因、错误代码、建议动作及复制诊断放在当前记录附近。封面失败有文字占位，预览入口为可聚焦按钮。
- 危险动作保留确认；后台任务只显示提交与真实状态，不暗示瞬时完成。

## 响应式与可访问性

- 1440px：固定侧栏、工作台双列、数据表格完整显示。
- 1024px：工作台内容转单列，仍保留桌面侧栏；表格可在容器内横向滚动。
- 390px：导航抽屉、表格字段卡片、单列作品、操作按钮保留文字。筛选项可以横向滚动但页面本身不能横向溢出。
- 所有仅图标按钮提供可读名称；输入有标签；键盘焦点明显，弹出侧栏可用 Escape 关闭并约束 Tab 顺序；尊重减少动态效果偏好。
- 目标为 WCAG 2.2 AA。真实数据与屏幕阅读器验收应在部署后重复执行，不能用本地静态构建代替。

## 维护规则

新增页面优先沿用导航、状态、按钮、表格和错误提示模式。不得把平台专项复制成新的首页，不得把接口失败显示为零、在线或“暂无数据”，不得将小红书作者主页批量采集重新开放。
